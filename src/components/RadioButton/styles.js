import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  buttonBody: {
    justifyContent: 'center',
    alignItems: 'center',
    width: 26,
    height: 26,
    borderRadius: 18,
  },
  deliveryAddTxt: {
    marginBottom: 16,
    flexDirection: 'row',
    alignItems: 'center',
  },
  txt: {
    marginLeft: 12,
  },
});
