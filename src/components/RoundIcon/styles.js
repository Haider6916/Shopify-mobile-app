import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  buttonBody: {
    justifyContent: 'center',
    alignItems: 'center',
    width: 48,
    height: 48,
    borderRadius: 24,
  },
});
