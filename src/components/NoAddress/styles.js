import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  main: {
    flex: 1,
    marginBottom: 15,
  },
  itemFound: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  emoji: {
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 70,
  
  },
  details: {
    justifyContent: 'center',
  },
});
